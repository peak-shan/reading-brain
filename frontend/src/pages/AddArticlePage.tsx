import { Typography } from "antd";

const { Title, Text } = Typography;

export default function AddArticlePage() {
  return (
    <div>
      <Title level={2}>➕ 添加文章</Title>
      <Text type="secondary">添加文章页 — 待开发</Text>
    </div>
  );
}
